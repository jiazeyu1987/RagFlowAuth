server {
  listen 80;
  server_name _;

  # Allow file uploads up to 16MB (matches backend MAX_FILE_SIZE)
  client_max_body_size 16m;

  # Ensure ES module scripts are served with correct MIME type (e.g. pdf.worker.min.mjs)
  location ~* \\.mjs$ {
    default_type application/javascript;
    try_files $uri =404;
  }

  # Frontend SPA
  root /usr/share/nginx/html;
  index index.html;

  # Use Docker's internal DNS resolver for dynamic name resolution
  resolver 127.0.0.11 valid=30s;
  set $backend_host "ragflowauth-backend:8001";

  # Reverse proxy to backend (same-origin, no CORS hassle)
  location /api/ {
    proxy_pass http://$backend_host;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location /health {
    proxy_pass http://$backend_host/health;
  }

  # SPA fallback
  location / {
    try_files $uri $uri/ /index.html;
  }
}
