# 软件需求规格（SRS）

版本: v1.2  
更新时间: 2026-04-03

| SRS ID | 对应 URS | 软件要求 | 主要实现证据 |
|---|---|---|---|
| SRS-001 | URS-001 | 权限解析以 `PermissionSnapshot` 为唯一来源，管理员文件操作权限固定收口。 | `backend/app/core/permission_resolver.py` |
| SRS-002 | URS-002 | 后端鉴权、前端按钮显隐、统一预览/浏览入口均按授权范围生效。 | `backend/app/core/authz.py`, `fronted/src/hooks/useAuth.js` |
| SRS-003 | URS-003 | 水印策略由后端下发，OnlyOffice 与统一预览层展示动态水印。 | `backend/database/schema/watermark_policy.py`, `fronted/src/shared/documents/preview/watermarkOverlay.js` |
| SRS-004 | URS-004 | 审批流程支持可配置层级、步骤级审批人与审批范围绑定、待我审批查询、审批/驳回时的当前步骤授权校验与状态留痕；旧文档审核链路全部并入统一审批中心。 | `backend/services/operation_approval/`, `backend/app/modules/operation_approvals/router.py`, `fronted/src/pages/ApprovalCenter.js`, `fronted/src/pages/ApprovalConfig.js` |
| SRS-005 | URS-005 | 通知服务需按当前审批节点解析收件人并按收件人逐通道建任务；待审批 payload 必须包含步骤名、目标对象和 `approval_target`，系统需支持站内信、外部通知、去重抑制、失败重试和管理员人工补发。 | `backend/services/notification/`, `backend/services/operation_approval/service.py`, `backend/app/modules/admin_notifications/`, `backend/services/knowledge_ingestion/manager.py` |
| SRS-006 | URS-006 | 审批/驳回动作必须携带签名挑战结果并落地验签记录，审批签名弹窗为审批中心通用组件。 | `backend/services/electronic_signature/`, `backend/app/modules/operation_approvals/router.py`, `fronted/src/features/operationApproval/SignatureConfirmModal.js` |
| SRS-007 | URS-007 | 系统必须对 R7 提供仓库内一致性门禁能力：受控 GMP 基线文档、周期复核状态、URS/SRS/风险/追踪矩阵/验证计划/验证报告的映射一致性、文档版本链和配置变更受控均需可校验；仓库外证据仅标记为 residual gap。 | `backend/services/compliance/r7_validator.py`, `scripts/validate_r7_repo_compliance.py`, `backend/database/schema/kb_documents.py`, `backend/app/modules/knowledge/routes/versions.py`, `backend/database/schema/config_change_logs.py`, `backend/services/config_change_log_store.py` |
| SRS-008 | URS-008 | 审计事件需记录资源、动作、前后值、原因、签名、请求 ID 和组织信息。 | `backend/database/schema/audit_logs.py`, `backend/services/audit/manager.py` |
| SRS-009 | URS-009 | 依赖注入应按 tenant/company 切换数据库连接，并提供迁移脚本。 | `backend/app/core/tenant.py`, `backend/database/tenant_paths.py`, `scripts/migrate_single_db_to_tenant_dbs.ps1` |
| SRS-010 | URS-010 | 备份任务完成后自动计算 `package_hash`；启用复制时复制失败必须使任务失败；恢复演练必须通过系统执行 hash 校验、必要文件校验、恢复后 `auth.db` 比对，并输出 `package_validation_status`、`acceptance_status` 与审计证据。 | `backend/services/data_security/backup_service.py`, `backend/services/data_security/replica_service.py`, `backend/services/data_security/restore_service.py`, `backend/app/modules/data_security/router.py`, `backend/database/schema/data_security.py`, `backend/database/schema/restore_drills.py` |
| SRS-011 | URS-011 | 系统必须提供仅管理员可调用的 `/api/audit/evidence-export`，输出 ZIP 取证包；包内至少包含审计日志、电子签名、审批动作、通知任务/回执、备份任务与恢复演练的 JSON/CSV 副本，以及 `manifest.json` 和 `checksums.json` 完整性摘要；导出动作必须写入审计日志。 | `backend/app/modules/audit/router.py`, `backend/services/audit/evidence_export.py`, `backend/services/audit/manager.py` |
| SRS-012 | URS-012 | 系统必须提供受控文档登记与审查包导出能力：`doc/compliance/controlled_document_register.md` 作为登记源，`/api/audit/controlled-documents` 用于列出当前受控文档，`/api/audit/review-package` 用于导出带 `review_package_manifest.json` 与 `review_package_checksums.json` 的最小审查包；发布版本映射不一致、文档头信息不一致或包分组不完整时必须被门禁阻断。 | `backend/app/modules/audit/router.py`, `backend/services/compliance/review_package.py`, `backend/services/compliance/fda03_validator.py` |
| SRS-013 | URS-013 | 系统必须提供维护影响判定能力，至少覆盖 `os / database / api / config / intended_use` 五类变更，并输出 `requires_revalidation`、`blocks_prior_validation`、`impacted_artifacts`、`traceability_refs`、`validation_status`；`config` 类别中 `upload_allowed_extensions` 和 `data_security_settings` 命中后必须要求再确认，`intended_use` 变化必须阻断继续沿用旧验证结论。 | `backend/services/compliance/gbz01_maintenance.py`, `backend/services/config_change_log_store.py` |
| SRS-014 | URS-014 | 系统必须提供紧急变更落库流程与状态机：`requested -> authorized -> deployed -> closed`；仅管理员可创建和部署，且必须校验 `authorizer_user_id != reviewer_user_id`；关闭前必须补齐 `post_review_summary`、`impact_assessment_summary`、`capa_actions` 和 `verification_summary`。 | `backend/database/schema/emergency_changes.py`, `backend/services/emergency_change.py`, `backend/app/modules/emergency_changes/router.py` |
| SRS-015 | URS-015 | 系统必须以现有退役记录链路实现 GBZ-03：`RetiredRecordsService` 负责生成退役文件副本、`retirement_manifest.json`、`checksums.json` 和记录包摘要；`/api/knowledge/retired-documents*` 负责保留期内受控查询/预览/下载；`/api/audit/retired-records*` 负责管理员清单查询与记录包导出；验证依据固定为 `backend.tests.test_retired_document_access_unit.py`，不引入第二套退役归档子系统。 | `backend/services/compliance/retired_records.py`, `backend/app/modules/knowledge/routes/retired.py`, `backend/app/modules/audit/router.py`, `backend/tests/test_retired_document_access_unit.py` |
| SRS-016 | URS-016 | 系统必须提供 `supplier_component_qualifications` 与 `environment_qualification_records` 两类持久化记录；管理员通过 `/api/supplier-qualifications/components*` 维护供应商/现成软件确认基线，通过 `/api/supplier-qualifications/environment-records` 维护环境级 IQ/OQ/PQ 结果；当 `current_version != approved_version` 时组件状态必须切换为 `requalification_required`，且 `tenant_database` 场景必须要求 `company_id`。 | `backend/database/schema/supplier_qualification.py`, `backend/services/supplier_qualification.py`, `backend/app/modules/supplier_qualification/router.py` |
| SRS-017 | URS-017 | 系统必须提供 `training_requirements`、`training_records`、`operator_certifications` 三类持久化记录；管理员通过 `/api/training-compliance/*` 维护培训要求、培训记录和操作员认证；审批中心的同意/驳回入口与恢复演练入口必须调用统一培训门禁，校验 `curriculum_version`、`effectiveness_status`、认证状态与有效期。 | `backend/database/schema/training_compliance.py`, `backend/services/training_compliance.py`, `backend/app/core/training_support.py`, `backend/app/modules/training_compliance/router.py`, `backend/app/modules/operation_approvals/router.py` |
