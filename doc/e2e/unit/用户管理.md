# 用户管理

## 全真链路状态
- 自动化状态：已接入 `doc/e2e` 全真链路
- 自动化用例：`fronted/e2e/tests/docs.user-management.spec.js`
- 数据来源：真实后端 API + 真实前端页面
- 约束：不使用 mock、不直改数据库冒充页面结果

## 自动化落地结果
- 已覆盖：
  - 管理员真实登录后，通过真实 API 创建用户
  - 在“用户管理”页面执行重置密码（UI 操作 + 后端生效）
  - 在“用户管理”页面执行停用/启用（UI 操作 + 后端生效）
  - 使用真实登录接口验证密码重置与启停状态的实际效果
  - 用例结束后真实删除临时账号，避免脏数据累积
- 未覆盖：
  - 通过“新建用户”弹窗逐字段填写并提交的 UI 创建路径
  - 权限组分配弹窗的完整回归链路
  - 目录树授权相关交互
