# 数据安全

## 全真链路状态

- 自动化状态：已接入 `doc/e2e` 全真链路
- 对应自动化：`fronted/e2e/tests/docs.data-security.spec.js`
- 运行入口：`python scripts\run_doc_e2e.py --repo-root .`
- 真实数据来源：页面配置保存、备份任务触发、备份任务查询和恢复演练查询全部走真实后端；如宿主机缺少 `ragflowauth-backend:latest`，用例会按真实 fail-fast 结果校验 `400 backup_worker_image_missing:ragflowauth-backend:latest`，不会伪造成功。
- 已确认前提：本地备份目录可写、真实 `auth_db_path` 已指向隔离库、真实 `ragflow_compose_path` 可读、Docker 可用且已发现 RAGFlow volumes。
- 禁止方案：不允许伪造备份成功状态、mock 备份列表或捏造恢复演练记录。

## 页面目标

用于保存数据安全配置、触发真实备份任务并记录真实恢复演练，保证关键数据可恢复、可追溯、可审计。

## 适用角色

- 管理员账号：当前侧边栏入口与真实验证均使用管理员。

## 前置条件

- 管理员可访问 `/data-security`。
- `target_local_dir` 指向可写的真实本地备份目录。
- `auth_db_path` 指向当前隔离环境的真实认证库。
- `ragflow_compose_path` 指向真实 `docker/docker-compose.yml`。
- Docker 服务可用，且存在 `ragflowauth-backend:latest` 备份工作镜像。
- 若要验证恢复演练，系统中至少已有一条状态为 `completed` 且包含 `package_hash` 的真实备份任务。

## 按钮测试项

### 保存保留策略

- 所在区域：保留策略配置区。
- 真实触发：管理员修改保留天数并点击“保存”。
- 实测结果：`PUT /api/admin/data-security/settings` 成功，刷新后仍保留最新值。

### 立即备份

- 所在区域：备份操作区。
- 真实触发：管理员点击“立即备份”。
- 当前实测结果：接口按 fail-fast 返回 `400 backup_worker_image_missing:ragflowauth-backend:latest`。
- 解锁条件：提供可用的 `ragflowauth-backend:latest` 镜像，使真实备份任务能够入库并进入终态。

### 全量备份

- 所在区域：备份操作区。
- 真实触发：管理员点击“全量备份”。
- 当前实测结果：接口按 fail-fast 返回 `400 backup_worker_image_missing:ragflowauth-backend:latest`。
- 解锁条件：与“立即备份”相同，需先满足真实备份工作镜像前提。

### 记录恢复演练

- 所在区域：恢复演练区域。
- 当前状态：受阻于上游真实备份任务未生成，当前环境无法执行真实恢复演练。
- 解锁条件：至少存在一条 `completed` 的真实备份任务，并在页面中可选中该任务后再提交演练记录。

## 后续扩展建议

- 已有真实 spec：`fronted/e2e/tests/docs.data-security.spec.js`
- 已有真实 helper：`fronted/e2e/helpers/securityToolsFlow.js`
- 当前已验证闭环：真实管理员登录 -> 真实保留策略保存 -> 真实备份接口调用 -> 真实 fail-fast blocker 暴露
- 待补齐事项：先补齐 `ragflowauth-backend:latest` 真实备份工作镜像并让真实 `backup_jobs` / `restore_drills` 落库；随后再由上游登记 `doc/e2e/manifest.json`
